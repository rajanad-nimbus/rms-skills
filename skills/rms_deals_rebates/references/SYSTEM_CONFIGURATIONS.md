# Vendor Deals System Options & Parameters

## 1. Core System Parameters
- `DEAL_CALC_FREQUENCY`: Billing calculation frequency (`M` = Monthly, `Q` = Quarterly).
- `DEAL_INCOME_ACCOUNTING_METHOD`: General Ledger revenue posting method (`I` = Reduce Item Cost, `O` = Other Income).
